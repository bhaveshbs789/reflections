What happens when you initialize a empty repo ? Why do you think that needs to be done ?
When we initialize a empty repo a .git file is created and it means we are ready to add tracking to our files.

